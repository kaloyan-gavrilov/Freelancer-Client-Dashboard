# Copy this file to .env and fill in real values.
# .env is git-ignored and must NEVER be committed.
# All three values are found in your Supabase project under Settings > API.

# The public URL of your Supabase project
SUPABASE_URL=https://your-project-ref.supabase.co

# anon/public key — respects Row Level Security policies
SUPABASE_ANON_KEY=your-anon-public-key

# JWT secret — used to verify tokens server-side without a round-trip to Supabase
SUPABASE_JWT_SECRET=your-jwt-secret

# Server
PORT=3000
NODE_ENV=development